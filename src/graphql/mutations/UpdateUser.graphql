mutation UpdateUser ($name: String!) {
    updateUser(name: $name) {
        errors {
            ...AuthErrorResponse
        }
        user {
            ...UserData
        }
        update
    }
}